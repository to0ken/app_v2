Структура проекта:



# FastAPI Application v2



## 📁 Структура проекта

```
app_v2/
├── api_v1/           # API версия 1
├── core/             # Основные настройки и утилиты
├── models/           # Модели базы данных
├── users/            # Модуль пользователей
│   ├── __init__.py
│   ├── crud.py      # Операции CRUD для пользователей
│   ├── schemas.py   # Pydantic схемы
│   └── views.py     # API эндпоинты пользователей
├── items_views.py    # API эндпоинты для items
├── main.py           # Точка входа в приложение
├── __init__.py
└── config.py         # Конфигурация приложения
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.8+
- pip (менеджер пакетов Python)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <ваш-репозиторий>
cd app_v2
```

2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv .venv
# Для Windows:
.venv\Scripts\activate
# Для Linux/Mac:
source .venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

Если файла `requirements.txt` нет, установите основные зависимости:
```bash
pip install fastapi uvicorn sqlalchemy python-dotenv
```

### Запуск приложения

#### Разработка (с автоперезагрузкой):
```bash
uvicorn main:app --reload
```

#### Продакшен:
```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

После запуска приложение будет доступно по адресу: [http://localhost:8000](http://localhost:8000)

## 📚 Документация API

FastAPI автоматически генерирует интерактивную документацию:

- **Swagger UI**: [http://localhost:8000/docs](http://localhost:8000/docs)


## 🔧 Конфигурация

Настройки приложения находятся в `config.py`. Для окружения разработки создайте файл `.env` в корне проекта:

```env
DATABASE_URL=sqlite:///./test.db
SECRET_KEY=your-secret-key-here
DEBUG=True
```

## 📦 Основные зависимости

- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с базами данных
- **Pydantic** - валидация данных и схемы
- **Uvicorn** - ASGI сервер
- **Python-dotenv** - работа с переменными окружения

## 🎯 Особенности

- Модульная архитектура
- Автоматическая документация API
- Поддержка версионирования API
- Готовые CRUD операции для пользователей
- Чистая структура проекта


### Добавление новых эндпоинтов

1. Создайте модуль в соответствующей директории
2. Добавьте схемы в `schemas.py`
3. Реализуйте бизнес-логику в `crud.py`
4. Создайте роутеры в `views.py`
5. Подключите роутер в `main.py`

### Пример структуры нового модуля:

```
new_module/
├── __init__.py
├── crud.py
├── schemas.py
└── views.py

## 👥 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Создайте Pull Request







